To run the testing environment, open SpecRunner.html in the web browser.

This will show what is running/ what isn't. Also shows why. Keep the jasmine folder. If we make a new js class, add it to SpecRunner.html.
